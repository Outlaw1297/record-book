# OAuth client IDs for Drive / Dropbox sync (public PKCE clients — no secrets).
# You can also paste these in Settings → Cloud sync on the device.
# See docs/sync-setup.md

VITE_GOOGLE_CLIENT_ID=
VITE_DROPBOX_APP_KEY=
